//These constants are used with the cache() function to save
//the state of a given page

const PAGE_SERVICES = 'page:services'
const PAGE_RELEASE = 'page:release'
const PAGE_PLAYLIST = 'page:playlist'
